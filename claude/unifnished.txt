Great thanks. Next 

Inside folder knowledge_sources/Networkgeekstuff there is a blog article in file Making AI accessing your personal work notes using RAG mechanism to be your work assistant.md, this one is already published about capability of this project to process knowledge sources as AI tools call using RAG. I want you to make a continuation of this article that will focuse on the new feature about extending the RAG with understanding images by preprocessing them with our preprocess_images.py that creates a side-car json meta data file for each image with a 1000 tokens long description of each image and together with context that is Obsidian directory structure and markdown context aware inserts it logically in the chunking process of RAG. Write  such article, focusing on using the preprocess_images.py, an example image is in knowledge_sources/acmeco/lab_documentation/ACME Co Network Lab Overview.md and the single image describing the network topology under knowledge_sources/acmeco/lab_documentation/images/lab_topology-Page-1.drawio.png and the metadata description of this topology in knowledge_sources/acmeco/lab_documentation/images/lab_topology-Page-1.drawio.png.meta.json .  This image should be considered the main example the article focuses on. At the end of the article, you can use just the test_rag.py which already inside on line 38 contains RAG test trying to retrive information about if adam kmet is owner of the labs erver called ubuntu2, which the metadata confirms. Alternativelly, you can actually start an LLM session using main.py and ask the question "Adam Kmet is owner of any acmeco lab server?" and the local LLM should be able to answer. 

As a side note at then, you can mention that for problems with RAG prioritization for small notes that are tipical for personal work notes, the project implemented two new tools for the LLM to use called searchKnowledgeFiles and readKnowledgeFile that are great to use instead of RAG and LLM can decide to us this when RAG prioritization doesnt find such small notes reliable. 

The new blog article should be placed in knowledge_sources/Netwworkgeekstuff/ as md file with formating compatible with wordpress blogs